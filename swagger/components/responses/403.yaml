description: Access forbidden. You don't get this information.
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
      properties:
        status:
          type: integer
          example: 403
        message:
          type: string
          example: Access forbidden
